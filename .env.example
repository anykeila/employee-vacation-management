# Copy to .env and adjust as needed. docker-compose reads .env automatically.
# Defaults are baked into docker-compose.yml, so the stack also runs without a .env.

POSTGRES_USER=vacation
POSTGRES_PASSWORD=vacation_dev_pwd
POSTGRES_DB=vacation
POSTGRES_PORT=5432

API_PORT=8080
ASPNETCORE_ENVIRONMENT=Development
